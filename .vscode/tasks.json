{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build cmake.",
            "type": "shell",
            "command": "./b.bat",
            // "command": "cmake --build ./build",
            // "command": "cmake --build ./build && cp stuff/kvs.json.1.bak stuff/kvs.json && echo 'everything ok'",
// && cp kvs.json.bak stuff/kvs.json && echo 'everything ok'
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}